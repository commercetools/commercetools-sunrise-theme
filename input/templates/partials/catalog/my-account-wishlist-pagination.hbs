<div class="my-account-wishlist-pagination">
  <div class="row">
    <div class="col-sm-4">
      <div class="my-account-wishlist-clear">
        <button>{{content.clearWishlistBtn}}</button>
      </div>
    </div>
    <div class="col-sm-4 text-center custom-pagination">
      <ul class="page-numbers">
        {{> catalog/pagination pagination=content.pagination}}
      </ul>
    </div>
    <div class="col-sm-4">
      <div class="my-account-wishlist-per-page">
        <span class="hidden-sm">{{i18n "catalog:displaySelector.title"}}</span>
        <select>
          {{#each content.displaySelector.list}}
            <option value="{{value}}" {{#if selected}}selected{{/if}}>{{label}}</option>
          {{/each}}
        </select>
      </div>
    </div>
  </div>
</div>
